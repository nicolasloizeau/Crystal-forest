//#region src/url.ts
function parseHashParams(url) {
	const hashIndex = url.indexOf("#");
	if (hashIndex === -1) return {
		params: "",
		lineage: []
	};
	const params = url.slice(hashIndex + 1);
	if (params.startsWith("0x")) return {
		params,
		lineage: []
	};
	const urlSearchParams = new URLSearchParams(params);
	let lineage = [];
	const lineageParam = urlSearchParams.get("lineage");
	if (lineageParam) lineage = lineageParam.split(",").map((l) => l.trim());
	return {
		params: urlSearchParams.get("params") || params,
		lineage
	};
}

//#endregion
export { parseHashParams };
//# sourceMappingURL=url-PF53DtLt.js.map